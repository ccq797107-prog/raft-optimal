# CD-Raft 端到端观测报告

生成时间：2026-06-09 02:03:02

本报告由 `TestScenarioReport` 自动生成，运行在真实 gRPC、注入了单向网络延迟的本地三域集群上。
集群**未被手工指定 leader**：每个节点在随机化超时后自行发起真实选举（真实投票、N-1 法定人数、任期 fencing），
因此下面记录的 Domain/Global Leader 是真正竞选胜出的节点，节点编号每次运行可能不同。
写入为基线两域提交（Fast Return 关闭），读取为 Global Leader 线性一致读；客户端到 Global Leader 的往返延迟按 origin 域注入。
每个延迟样本失败会自动重试，若始终无法取得成功样本测试将直接失败，因此本表不会出现“全 0”的占位值。

## 拓扑与延迟矩阵

三个域 `a`/`b`/`c`，每域 3 节点（`a1..a3` 等）。本地单向延迟 1ms，跨域单向延迟（往返为其两倍）：

| 链路 | 单向延迟 | 往返 RTT |
|---|---|---|
| `a <-> b` | 50ms | 100ms |
| `a <-> c` | 80ms | 160ms |
| `b <-> c` | 60ms | 120ms |

## 阶段 0：初始选举（自发，无人工指定）

- Domain Leader 选举结果：域 `a` → `a1`，域 `b` → `b3`，域 `c` → `c2`
- Global Leader：**b3**（域 `b`），全局任期 `1`
- 各域 Domain Leader 与 Global Leader 均由随机化超时下的真实选举产生（重跑会变化）。

## 场景一：单域持续最优（稳定，无迁移）

负载始终来自 Global Leader 所在域 `b`，最优位置不变，优化器不应触发迁移。

- 负载来源域：`b`
- 外部控制器推荐域：``（预测 L_s=0.0）
- Global Leader 保持不变：**`b3`（域 `b`，任期 `1`）**
- 迁移计数（发起方 `b3` 视角）：`0`

候选域预测成本（论文 L_s 模型，外部控制器计算，越低越优）：

| 候选域 | 预测 L_s | 可提交 |
|---|---|---|
| `a` | 0.0 | false |
| `b` | 0.0 | false |
| `c` | 0.0 | false |


客户端读写延迟（按 origin 域，中位数 / 平均，每格 6 个成功样本；写为两域提交，读为线性一致读）：

| client origin | 与 GL 同域 | 写中位数 | 写平均 | 读中位数 | 读平均 |
|---|---|---|---|---|---|
| `a` | 否 | 213ms | 213ms | 101ms | 101ms |
| `b` | **是** | 114ms | 114ms | 3ms | 3ms |
| `c` | 否 | 233ms | 233ms | 121ms | 121ms |


## 场景二：最优域切换（触发安全交接迁移）

负载全部来自域 `a`（与现任 Global Leader 域 `b` 不同），优化器应推荐 `a` 并通过 catch-up 安全交接把 Global Leader 迁移过去。

- 负载来源域：`a`
- 外部控制器推荐域：`b`（预测 L_s=3240.0）
- Global Leader 迁移：`b3`（域 `b`，任期 `1`）→ **`a1`（域 `a`，任期 `2`）**
- 迁移计数（发起方 `b3` 视角）：`1`
- 迁移记录：`migrated to a term 2 after catch-up to barrier 41 (scenario-report move)`

候选域预测成本（论文 L_s 模型，外部控制器计算，越低越优）：

| 候选域 | 预测 L_s | 可提交 |
|---|---|---|
| `a` | 3720.0 | true |
| `b` | 3240.0 | true |
| `c` | 4080.0 | true |


客户端读写延迟（按 origin 域，中位数 / 平均，每格 6 个成功样本；写为两域提交，读为线性一致读）：

| client origin | 与 GL 同域 | 写中位数 | 写平均 | 读中位数 | 读平均 |
|---|---|---|---|---|---|
| `a` | **是** | 115ms | 115ms | 3ms | 3ms |
| `b` | 否 | 216ms | 215ms | 102ms | 102ms |
| `c` | 否 | 276ms | 274ms | 161ms | 161ms |


## 结论

- 稳定场景下负载与 Global Leader 同域，优化器保持推荐当前域，未发生迁移。
- 负载迁移到新域后，优化器识别出更优位置并通过安全交接把 Global Leader 迁移过去；迁移后该域的读写延迟显著下降（读取从一个跨域 RTT 降为本地，写入从约两个跨域 RTT 降为约一个）。
- 全程未直接改写 leader 指针，迁移仍走更高任期选举 + N-1 + 日志最新性 + fencing；leader 位置由真实选举决定。
