\* Buggy three-domain run: PerDomainOnce dropped. A domain with two simultaneous
\* Domain Leaders double-votes, so TLC finds a reachable dual-Global-Leader state.
\* Expected: OneGlobalVotePerDomainPerTerm / GlobalLeaderUniqueness violated.
CONSTANTS
    Domains = {"A", "B", "C"}
    Servers = {"a1", "a2", "a3", "b1", "b2", "b3", "c1", "c2", "c3"}
    MaxDTerm = 2
    MaxGTerm = 1
    PerDomainOnce = FALSE

SPECIFICATION Spec

INVARIANT TypeOK
INVARIANT DomainLeaderUniqueness
INVARIANT OneGlobalVotePerDomainPerTerm
INVARIANT GlobalLeaderUniqueness
